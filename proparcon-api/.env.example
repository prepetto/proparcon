# =========================
# PROPARCON API - ENV EXAMPLE
# Copia este archivo como ".env" (local) o ".env.supabase" (cloud)
# Nunca subas ".env" real al repositorio.
# =========================

# --- PostgreSQL (local Docker) ---
POSTGRES_USER=admin
POSTGRES_PASSWORD=CHANGEME_LOCAL
POSTGRES_DB=proparcon
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_SCHEMA=proparcon

# --- PostgreSQL (Supabase) ---
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=CHANGEME_SUPABASE
# POSTGRES_DB=postgres
# POSTGRES_HOST=YOURPROJECTREF.supabase.co
# POSTGRES_PORT=5432
# POSTGRES_SCHEMA=proparcon

# --- Seguridad / JWT (si tu settings lo usa) ---
JWT_SECRET_KEY=CHANGEME_SUPER_SECRET
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# --- App ---
ENV=local
LOG_LEVEL=INFO
